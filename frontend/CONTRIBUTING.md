# Contributing

## Development Setup

```bash
npm install
npm run dev:local
```

## Pull Request Expectations

- Keep PRs focused and reviewable.
- Run `npm run lint` and `npm run build` before opening PR.
- Update docs when changing API contracts or UX behavior.
- Do not commit local env files or generated artifacts.
